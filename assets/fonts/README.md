# Fonts

Certificate details are printed in Times New Roman. The renderer looks here
first, then falls back to the system fonts (Times New Roman, Liberation Serif,
DejaVu Serif).

Drop a font in this folder as `TimesNewRoman.ttf` if none of those are installed
on the machine running the app.
